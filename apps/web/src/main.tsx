import { createRoot } from "react-dom/client";

createRoot(document.getElementById("root")!).render(
  <main><h1>Aegis Fleet</h1><p>Zero-trust security control plane for enterprise AI agents.</p></main>,
);
