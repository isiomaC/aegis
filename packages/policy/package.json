{
  "name": "@aegis/policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "pnpm --filter @aegis/contracts build && tsc --noEmit"
  },
  "dependencies": {
    "@aegis/contracts": "workspace:*"
  }
}
